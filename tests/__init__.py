"""Local test helpers for the Lab 21 test suite.

Keeping ``tests`` as an explicit package prevents an unrelated site-packages module
with the same name from shadowing ``tests.fake_tokenizer`` on some environments.
"""
